# database/ (Java package)
Owner: Team Leader + Co-Developer, built once /database/schema.sql exists.
JDBC connection handling, load/save between the database and the
in-memory custom data structures.

STATUS: empty — waiting on /database/schema.sql
