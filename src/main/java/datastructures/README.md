# datastructures/
Owner: Team Leader + Co-Developer, built from /specs/data-structures/

One custom implementation per required structure (dynamic array, linked list,
stack, queue/circular queue, deque, priority queue/heap, BST, red-black/balanced
tree, B-tree, hash table, set/map, disjoint set, graph). No built-in HashMap,
TreeMap, PriorityQueue, Stack or ArrayDeque for this assessed core logic.

STATUS: empty — waiting on specs from /specs/data-structures/
