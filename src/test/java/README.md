# test/java/
JUnit tests built from /testing/test-plan.md — at least 40 tests covering
normal, boundary and invalid-input cases across all structures/algorithms.

STATUS: empty — waiting on /testing/test-plan.md
