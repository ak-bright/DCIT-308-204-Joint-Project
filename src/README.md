# src/ — Owner: Bright (Group Leader), generated via Claude Code

All Java source lives here: datastructures/, algorithms/, database/ (JDBC layer),
model/ (plain data classes), ui/ (console menu), and test/java/ (JUnit tests).
No other team member needs to edit anything in this folder.

STATUS: empty — populated by Claude Code from CLAUDE_CODE_PROMPT.md
